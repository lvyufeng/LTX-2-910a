[project]
name = "ltx2-ascend-ops"
version = "0.1.0"
description = "Optional AscendC custom operators for LTX-2 Ascend inference"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.8,<0.10.0"]
build-backend = "uv_build"
